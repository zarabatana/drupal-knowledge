# Cases

`cases/solved/` stores verified Drupal solved cases.

The schema and lifecycle exist so cases solved by the maintainers can be
captured without confusing internal proof with universal Drupal rules.
