# Source State

This directory stores the last normalized content hash for each collected source.

State files are generated by `collectors/collect.py` and must point to an immutable
snapshot whose filename is the SHA-256 digest of the normalized snapshot bytes.
